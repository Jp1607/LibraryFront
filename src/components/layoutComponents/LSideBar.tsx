import { LButton } from "../buttons/LButton"
import { LBox } from "./LBox"

const LSideBar = () => {

    const handleNavigation = (path: string) => {

    }

    return (
        <LBox>

        </LBox>
    )
}